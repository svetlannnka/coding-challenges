############ Work with strings and dictionaries ############

# 1. Create a function count_letters(word) that will take a string
# and return a dictionary in this format {letter: letter_count}.
# For example:
# count_letters(‘cat’) => should return {‘c’: 1, ‘a’: 1, ‘t’: 1}
# count_letters(‘tacocat’) => should return {‘t’: 2, ‘a’: 2, ‘c’: 2, ‘o’: 1}


# 2. Create a function unique_letters(word) that will take a string
# and return a list of unique letters only:
# For example:
# unique_letters(‘cat’) => should return ['c', 'a', 't']
# unique_letters(‘tacocat’) => should return ['o']


# 3. Create a function first_unique_letter(word) that will take a string
# and return only the first unique letter:
# For example:
# first_unique_letter(‘cat’) => should return 'c'
# unique_letters(‘tacocat’) => should return 'o'


# 4. Create a function most_common_letters(word) that will take a string
# and return the most common letter(s) as a list:
# For example:
# most_common_letters(‘cat’) => should return ['c', 'a', 't']
# most_common_letters(‘will’) => should return ['l']